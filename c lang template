#include <stdio.h>
#include<string.h>
#define ll long long
#define cout(x) printf("%d\n",x);
#define cin(x) scanf("%d",&x);
#define couts(x) printf("%s\n",x);
#define coutc(x) printf("%c\n",x);
#define input(end,arr)  for(int i = 0; i<end ; i++){cin(arr[i]);}
#define fol(i,end)  for(int i = 0; i<end ; i++)
#define prinarr(end,arr)  for(int i = 0; i<end ; i++){cout(arr[i]);}
#define swapn(a, b) {int temp = a;a = b;b = temp;} 
#define dbg(x) printf("DEBUG: %s = %d\n", #x, x) 
#define string char*

int solve(){

}

int main(){

int t;
cin(t);
while(t--){
    solve();
}

return 0;
}
